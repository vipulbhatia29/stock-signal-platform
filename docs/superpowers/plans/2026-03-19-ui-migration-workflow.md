# UI Migration Workflow — Lovable → Production

> **Generated by:** `/sc:workflow` (Session 37)
> **Input:** `docs/lovable/migration-gap-analysis.md` + `docs/lovable/functionaldesign/StockSignal_Functional_Design_Spec.md`
> **Reference code:** `docs/lovable/code/stocksignal-source/`
> **Live prototype:** https://stocksignal29.lovable.app
> **Strategy:** Systematic, depth=deep

---

## Execution Order

```
Step 0: Critical Bug Fixes (KAN-55/56)          ← MUST DO FIRST
Step 1: Phase 4E Security Fixes                  ← quick wins
Step 2: Phase 4C.1 Code Quality + Performance    ← stabilize before UI work
Step 3: Phase UI-1 — Shell + Design Tokens       ← foundation for all pages
Step 4: Phase UI-2 — Shared Components           ← reusable across pages
Step 5: Phase UI-3 — Dashboard Redesign          ← highest-impact page
Step 6: Phase UI-4 — Screener + Stock Detail     ← data-dense pages
Step 7: Phase UI-5 — Portfolio Redesign          ← existing page facelift
Step 8: Phase UI-6 — Sectors Page (NEW)          ← new page + backend
Step 9: Phase UI-7 — Auth Redesign              ← entry point polish
Step 10: Phase UI-8 — Chat Panel Polish          ← final refinement
Step 11: Phase UI-9 — Animations + Final Polish  ← framer-motion + glows
```

---

## Step 0: Critical Bug Fixes (~1.5 hours)

**Priority:** HIGHEST — chat is broken without this
**Branch:** `hotfix/KAN-55-tool-fixes` → develop

### 0.1 KAN-55: Fix 4 Tool Wrapper Bugs
- [ ] Thread `user_id` from `chat_stream` → `config["configurable"]["user_id"]` → `execute_tool_safely`
- [ ] Fix `analyze_stock` wrapper: call `load_prices_df(session, ticker)` then `compute_signals(ticker, df)`
- [ ] Fix `compute_signals_tool` wrapper: same pattern as analyze_stock
- [ ] Fix `portfolio_exposure` wrapper: look up `portfolio_id` from `user_id`, pass to `get_portfolio_summary()`
- [ ] Fix `recommendations_tool` wrapper: fetch `current_price` from DB via `get_latest_price()`
- [ ] Test: curl `POST /chat/stream` with tool-triggering questions — verify tool_result events stream back
- [ ] Run full test suite (unit + API + frontend)

### 0.2 KAN-56: Fix Index Seeding Script
- [ ] Add User-Agent header to `pandas.read_html()` call in `scripts/sync_sp500.py` and `scripts/sync_indexes.py`
- [ ] Verify: `uv run python scripts/sync_indexes.py` → non-zero membership counts
- [ ] Run seed to populate dev DB

**Checkpoint:** Chat tools work end-to-end. Market indexes populated.

---

## Step 1: Phase 4E Security Fixes (~20 min)

**Branch:** `hotfix/KAN-4E-security` → develop

- [ ] Apply `MCPAuthMiddleware` to `/mcp` mount in `backend/main.py`
- [ ] Add `user_id` ownership check in `chat_stream` + `get_session_messages`
- [ ] Replace `str(exc)` with generic message in `stream.py` error handler
- [ ] Replace `str(exc)` with generic message in delete session 403
- [ ] 4 tests: MCP 401, session IDOR 403, error event sanitized, delete 403 sanitized

**Checkpoint:** Security review clean. All endpoints enforce ownership.

---

## Step 2: Phase 4C.1 — Code Quality + Performance (~2 hours)

**Branch:** `feat/KAN-4C1-polish` → develop

### 2.1 Functional Fixes
- [ ] Wire `csvData` prop from `MessageBubble` → `MessageActions`
- [ ] Session expiry prompt when clicking expired session
- [ ] localStorage session restore on mount
- [ ] Fix `tool_calls` type hint: `dict | None` → `list[dict] | None`

### 2.2 Code Quality Fixes
- [ ] `crypto.randomUUID()` instead of `let nextId = 0`
- [ ] Add `user: User` type annotation to all 4 chat endpoints
- [ ] Add OpenAPI `summary`/`description` to chat endpoints
- [ ] Guard `app.state.*_graph` with `getattr()` + 503
- [ ] Fix `data: Any` → `dict[str, Any] | None` on StreamEvent
- [ ] Add `CLEAR_ERROR` action type to reducer
- [ ] Move lazy imports to top of chat router
- [ ] Extract `get_session()` function to `chat_session.py`

### 2.3 Performance Fixes
- [ ] Hoist ReactMarkdown plugin arrays to module constants
- [ ] Gate artifact dispatch `useEffect` with `!isStreaming`
- [ ] Fix stale `activeSessionId` with ref
- [ ] `React.memo()` on MessageBubble
- [ ] Remove `dispatch` from `useStreamChat` return

**Checkpoint:** All 429 tests pass. Lint clean. No code quality issues.

---

## Step 3: Phase UI-1 — Shell + Design Tokens (~3 hours)

**Branch:** `feat/KAN-UI1-shell` → develop
**Reference:** Lovable `ShellLayout.tsx`, `SidebarNav.tsx`, `Topbar.tsx`, `index.css`, `tailwind.config.ts`

### 3.1 Design Token Updates
- [ ] Add to `globals.css`: `--hov`, `--card2-foreground`, `--subtle`, `--cyan-muted`, sidebar tokens
- [ ] Add to `tailwind.config.ts` (or equivalent): `hov`, `card2` foreground, `cyan.muted`, `gain`/`loss`/`warning` foreground variants, sidebar namespace
- [ ] Add animations: `pulse-subtle` (opacity 0.6→1 2s), `blink` (opacity step-end 1s)
- [ ] Add `scrollbar-thin` utility class
- [ ] Install framer-motion: `cd frontend && npm install framer-motion`

### 3.2 Sidebar Redesign
- [ ] Add logo image with `drop-shadow` cyan glow (need logo asset — use Lovable's or create)
- [ ] Add Sectors nav item (`PieChart` icon) linking to `/sectors`
- [ ] Implement active left cyan bar indicator (`w-0.5 bg-primary`)
- [ ] Replace avatar logout with dedicated logout button (destructive hover: `hover:bg-destructive/10`)
- [ ] Switch CSS tooltips to shadcn Tooltip component
- [ ] Update tests

### 3.3 Topbar Redesign
- [ ] Centered search bar with `<kbd>/</kbd>` keyboard hint badge
- [ ] Embed Refresh All (`RefreshCw`) button inside search bar
- [ ] Pulsing green dot on market status (reuse `isNYSEOpen()`, add `animate-pulse-subtle`)
- [ ] Signal count with `Activity` icon badge
- [ ] Notification bell icon (stub — no backend, tooltip "Coming soon")
- [ ] AI Analyst toggle with active state: `bg-primary/10 border-primary/30 shadow-[0_0_20px_hsl(var(--primary)/0.25)]`
- [ ] Update tests

### 3.4 ChatContext for Grid Adaptation
- [ ] Create `ChatContext` (or extend existing): `chatOpen`, `toggleChat()`
- [ ] Wire into authenticated layout — replace local `chatIsOpen` useState
- [ ] Export `useChat()` hook for pages to read `chatOpen`

**Checkpoint:** Shell looks like Lovable prototype. All existing pages still render correctly.

---

## Step 4: Phase UI-2 — Shared Components (~2 hours)

**Branch:** `feat/KAN-UI2-components` → develop
**Reference:** Lovable `shared/` components

### 4.1 New Components
- [ ] `ScoreBar` — 10-segment bar, color-coded by score threshold
- [ ] `RefreshIndicator` — per-stock freshness, relative time, click to refresh, spinning animation, stale detection
  - Needs: `last_fetched_at` exposed in API responses (backend change)
  - Backend: add `last_fetched_at` to `WatchlistItem`, `BulkSignalItem` schemas
- [ ] `IndexCard` — market index card with sparkline, value, change%, stock count

### 4.2 Updated Components
- [ ] `ScoreBadge` — match Lovable sizing (xs/sm/md), mono font, tabular-nums
- [ ] `SignalBadge` — add GOLDEN_CROSS, DEATH_CROSS, ABOVE_200, BELOW_200 signal labels
- [ ] `ChangeIndicator` — add dollar prefix option, ensure mono font
- [ ] `AllocationDonut` — add "Click to explore sectors →" link variant
- [ ] `StatTile` — add 2px accent gradient top border, donut variant, signal-summary variant

### 4.3 Tests
- [ ] Jest tests for ScoreBar, RefreshIndicator, IndexCard
- [ ] Update existing component tests for modified props

**Checkpoint:** Shared component library complete. All components match Lovable design.

---

## Step 5: Phase UI-3 — Dashboard Redesign (~3 hours)

**Branch:** `feat/KAN-UI3-dashboard` → develop
**Reference:** Lovable `Dashboard.tsx`

### 5.1 Layout Restructure
- [ ] KPI Stat Tiles (5-col grid, 3-col when chat open)
- [ ] Market Indexes section (3 IndexCards — S&P 500, NASDAQ-100, Dow 30)
  - Backend: need index data from DB (depends on KAN-56 seed fix)
  - If no index data: show friendly empty state (not "run a script")
- [ ] Action Required section (2/3 width) + Sector Allocation (1/3)
  - Recommendation rows with action icon, confidence badge, reasoning text, composite score
  - Backend: add `reasoning` field to recommendation generation (simple signal-based text)
  - "Held" badge cross-referencing portfolio
- [ ] Watchlist section (4-col grid, 3-col when chat open)
  - Cards with: Ticker + Held badge + ScoreBadge + Sparkline + Price + ChangeIndicator + ScoreBar + SignalBadge + RefreshIndicator

### 5.2 Backend Changes
- [ ] Add `last_fetched_at` to watchlist API response
- [ ] Add `reasoning: str` to recommendation generation (template-based from signals)
- [ ] Add `GET /api/v1/indexes` endpoint returning index values + sparkline (or use mock if seed not ready)

### 5.3 Chat-Open Grid Adaptation
- [ ] Dashboard reads `useChat().chatOpen` → adjusts grid: 5→3 cols for KPI, 4→3 for watchlist

### 5.4 Tests + Verification
- [ ] Update dashboard page tests
- [ ] Visual verification via Playwright

**Checkpoint:** Dashboard matches Lovable layout with live data.

---

## Step 6: Phase UI-4 — Screener + Stock Detail (~3 hours)

**Branch:** `feat/KAN-UI4-screener-detail` → develop

### 6.1 Screener Updates
- [ ] Remove Score range slider, add ScoreBar + ScoreBadge inline per row
- [ ] Add "Fresh" column with RefreshIndicator per stock
- [ ] Add "Held" badge (Briefcase icon) for portfolio stocks
- [ ] Add Performance tab columns: Annual Return, Volatility, Sharpe (data already in API)
- [ ] Replace DensityProvider with simple Compact/Comfortable button toggle
- [ ] Filter Reset button with `RotateCcw` icon
- [ ] Chat-open grid adaptation for grid view

### 6.2 Stock Detail Updates
- [ ] Header: Close button, large price display, dollar change, RefreshIndicator
- [ ] Watchlist toggle button (BookmarkCheck/Bookmark)
- [ ] **Candlestick chart toggle** — Line/Candle pill buttons
  - Backend: add `GET /api/v1/stocks/{ticker}/prices?format=ohlc` or include OHLC in existing response
- [ ] **Benchmark comparison chart** — stock vs S&P 500 vs NASDAQ (% change)
  - Backend: `GET /api/v1/stocks/{ticker}/benchmark` or fetch ^GSPC/^IXIC prices
  - Alternative: client-side fetch from yfinance proxy if backend too complex
- [ ] Signal cards: add description text ("Between 30-70 — balanced momentum")
- [ ] Signal cards: add RSI meter bar (0-100 gauge)

### 6.3 Tests
- [ ] Update screener + stock detail component tests
- [ ] Playwright: verify candlestick toggle, benchmark chart

**Checkpoint:** Screener and stock detail match Lovable.

---

## Step 7: Phase UI-5 — Portfolio Redesign (~2 hours)

**Branch:** `feat/KAN-UI5-portfolio` → develop

### 7.1 Portfolio Value Chart
- [ ] Add cost basis dashed overlay line on portfolio value chart

### 7.2 Positions Table
- [ ] Alert icons: `AlertOctagon` (critical/red) + `AlertTriangle` (warning/amber) instead of text badges
- [ ] Sector concentration warning banner: `bg-loss/10 border border-loss/20`

### 7.3 Settings Sheet
- [ ] Replace shadcn Sheet with framer-motion slide-in (`initial={{ x: "100%" }}`)
- [ ] Add `backdrop-blur-sm` + `bg-background/60` overlay

### 7.4 Transaction Modal
- [ ] Replace dialog with centered modal with `scale` animation
- [ ] BUY/SELL toggle: active BUY = `bg-gain/15 border-gain/25 text-gain`

### 7.5 Tests
- [ ] Update portfolio component tests

**Checkpoint:** Portfolio page matches Lovable.

---

## Step 8: Phase UI-6 — Sectors Page (NEW) (~4 hours)

**Branch:** `feat/KAN-UI6-sectors` → develop

### 8.1 Backend: Sectors Endpoint
- [ ] `GET /api/v1/sectors` — stocks grouped by sector, each with: sector name, stock count, avg composite score, avg annual return, list of tickers
- [ ] `GET /api/v1/sectors/{sector}/stocks` — full stock data for a sector (signals, performance)
- [ ] `GET /api/v1/sectors/{sector}/correlation` — price correlation matrix (pandas `.corr()` on price histories)
- [ ] Unit + API tests for all 3 endpoints

### 8.2 Frontend: Sectors Page
- [ ] Create `frontend/src/app/(authenticated)/sectors/page.tsx`
- [ ] Breadcrumb: Dashboard > Sectors
- [ ] Header: "Sector Performance" title + Portfolio Only toggle + Correlation toggle
- [ ] AllocationDonut overview (full sector breakdown)
- [ ] Sector accordion cards (collapsible):
  - Header: color dot, name, stock count, avg score (color-coded), avg return (ChangeIndicator)
  - Expanded: full comparison table (ticker, name, price, change, score, RSI, MACD, SMA, return, volatility, sharpe, refresh)
  - Portfolio stocks highlighted with `bg-primary/5`

### 8.3 Correlation Matrix (within sector accordion)
- [ ] Heatmap view: CSS grid, color-coded cells (green → red by correlation)
- [ ] Table view: select ticker → ranked correlation list with interpretation text
- [ ] Legend: 5-range color bar with diversification explanation

### 8.4 Tests
- [ ] Backend: 6+ tests (sectors endpoint, correlation computation, edge cases)
- [ ] Frontend: Sectors page render test, accordion expand test

**Checkpoint:** Sectors page functional with real data. Correlation matrix works.

---

## Step 9: Phase UI-7 — Auth Redesign (~2 hours)

**Branch:** `feat/KAN-UI7-auth` → develop

### 9.1 Split-Panel Layout
- [ ] Left panel: brand showcase (hidden on mobile)
  - Logo with `drop-shadow` glow
  - 3 feature bullets (Real-time Signals, Deep Analytics, Portfolio Guard)
  - Animated sparkline SVG decoration
  - Gradient grid background + glowing orbs (`blur-[120px] bg-primary/10`)
- [ ] Right panel: auth form

### 9.2 Login Form
- [ ] Google OAuth button (UI stub — no backend, shows toast "Coming soon")
- [ ] "OR" divider
- [ ] AuthInput component: icon prefix + label + focus glow ring
- [ ] "Remember me" checkbox + "Forgot password?" link (stub)
- [ ] Sign in button with cyan glow `shadow-[0_0_20px_hsl(var(--primary)/0.25)]`

### 9.3 Register Form
- [ ] Same layout with Full Name field
- [ ] "Create account" button

### 9.4 Tests
- [ ] Update auth page tests

**Checkpoint:** Auth pages match Lovable. Login flow still works.

---

## Step 10: Phase UI-8 — Chat Panel Polish (~1.5 hours)

**Branch:** `feat/KAN-UI8-chat` → develop

### 10.1 Agent Selector
- [ ] "Choose an Agent" heading with bot icon
- [ ] Styled cards with icons (BarChart3 for Stock, Globe for General)
- [ ] Descriptive subtitles: "Signals, portfolio, SEC filings, macro" / "News & web search only"

### 10.2 Suggestions
- [ ] Change behavior: chips fill input text (don't auto-send)
- [ ] Add Sparkles icon above chips

### 10.3 Tool Cards
- [ ] Truncate params display (don't show raw JSON)
- [ ] Add Copy + CSV buttons in expanded state

### 10.4 Markdown Rendering
- [ ] Custom table renderer (styled `<table>` with borders, alternating row colors)

### 10.5 Thinking Indicator
- [ ] 3 pulsing cyan dots with `animate-pulse-subtle`

### 10.6 Tests
- [ ] Update chat panel tests

**Checkpoint:** Chat panel matches Lovable UX.

---

## Step 11: Phase UI-9 — Animations + Final Polish (~1.5 hours)

**Branch:** `feat/KAN-UI9-polish` → develop

### 11.1 Framer Motion Animations
- [ ] Staggered fade-up on all card grids: `initial={{ opacity: 0, y: 12 }}` with `delay: i * 0.05`
- [ ] Slide-in for settings sheet
- [ ] Scale-in for modals
- [ ] Row-by-row fade in screener table

### 11.2 Glow Effects
- [ ] CTA buttons: `shadow-[0_0_20px_hsl(var(--primary)/0.25)]`
- [ ] Input focus: `focus-within:shadow-[0_0_0_2px_hsl(var(--primary)/0.1)]`

### 11.3 Scrollbar
- [ ] `scrollbar-thin` utility on main content area

### 11.4 Chat-Open Grid Adaptation
- [ ] Dashboard, Screener, Stock Detail, Portfolio, Sectors all read `chatOpen` and adjust grid columns

### 11.5 Full E2E Verification
- [ ] Playwright tour: all 7 pages (login, register, dashboard, screener, stock detail, portfolio, sectors)
- [ ] Visual comparison against Lovable prototype
- [ ] All tests pass (backend + frontend)

**Checkpoint:** Full platform matches Lovable design. Production-ready UI.

---

## Dependency Graph

```
KAN-55 (tool bugs) ──┐
KAN-56 (index seed) ──┤
                      ├── Phase 4E (security) ──── Phase 4C.1 (quality) ──┐
                      │                                                    │
                      │              ┌─────────────────────────────────────┘
                      │              │
                      │              ├── UI-1 (shell + tokens) ──── UI-2 (shared components) ──┐
                      │              │                                                          │
                      │              │         ┌───────────────────────────────────────────────┘
                      │              │         │
                      │              │         ├── UI-3 (dashboard) ─┐
                      │              │         ├── UI-4 (screener)  ─┤
                      │              │         ├── UI-5 (portfolio) ─┤
                      │              │         │                     │
                      │              │         │  UI-6 (sectors) ───┤  ← needs UI-2 components
                      │              │         │  UI-7 (auth) ──────┤  ← independent
                      │              │         │                     │
                      │              │         └── UI-8 (chat) ─────┤
                      │              │                              │
                      │              │                              └── UI-9 (animations + polish)
```

**Parallelizable:** UI-3, UI-4, UI-5, UI-7 can run in parallel after UI-2 is done.
**Sequential:** UI-1 → UI-2 must be sequential (tokens + components used by pages).
**Backend-dependent:** UI-3 (recommendations reasoning), UI-4 (benchmark data), UI-6 (sectors endpoint).

---

## Estimated Effort

| Step | Phase | Effort | Backend? |
|------|-------|--------|----------|
| 0 | Bug fixes (KAN-55/56) | 1.5h | Yes |
| 1 | Phase 4E security | 0.3h | Yes |
| 2 | Phase 4C.1 quality | 2h | Yes (minor) |
| 3 | UI-1 Shell + tokens | 3h | No |
| 4 | UI-2 Shared components | 2h | Yes (last_fetched_at) |
| 5 | UI-3 Dashboard | 3h | Yes (recommendations, indexes) |
| 6 | UI-4 Screener + Detail | 3h | Yes (OHLC, benchmark) |
| 7 | UI-5 Portfolio | 2h | No |
| 8 | UI-6 Sectors (NEW) | 4h | Yes (3 new endpoints) |
| 9 | UI-7 Auth | 2h | No |
| 10 | UI-8 Chat polish | 1.5h | No |
| 11 | UI-9 Animations | 1.5h | No |
| | **Total** | **~26h** | |

At 4-5h per session: **~5-6 sessions** to complete the full migration.

---

## JIRA Structure

```
EPIC: Phase 4F — UI Migration (Lovable → Production)
├── Story: UI-1 Shell + Design Tokens
├── Story: UI-2 Shared Components
├── Story: UI-3 Dashboard Redesign
├── Story: UI-4 Screener + Stock Detail
├── Story: UI-5 Portfolio Redesign
├── Story: UI-6 Sectors Page (NEW)
├── Story: UI-7 Auth Redesign
├── Story: UI-8 Chat Panel Polish
└── Story: UI-9 Animations + Final Polish
```

**Note:** Steps 0-2 (bug fixes, security, quality) are already tracked as KAN-55, KAN-56, Phase 4E, Phase 4C.1.

---

## Open Questions — Resolved

| Question | Decision |
|----------|----------|
| Google OAuth | UI stub now (toast "Coming soon"), backend OAuth deferred to Phase 5+ |
| Correlation matrix | Include in UI-6. Compute server-side via pandas `.corr()` |
| Notification bell | Stub icon with tooltip "Coming soon". Backend in Phase 5 |
| Framer Motion | Install in UI-1. Use throughout. ~30KB gzipped is acceptable |
| Candlestick data | OHLC exists in DB. Add `format=ohlc` query param to prices endpoint |
| Recommendation reasoning | Template-based from signal data (e.g., "Bullish MACD with stable RSI"). Not LLM-generated |

---

## Next Step

After this plan is approved, use `/sc:implement` or `superpowers:executing-plans` to execute step by step. Start with Step 0 (KAN-55/56 bug fixes) in the next session.
